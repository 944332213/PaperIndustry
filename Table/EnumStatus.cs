namespace Lushi.PaperProducts.Model.Table
{
    public static class EnumStatus
    {
        public enum Value
        {
            CheckPending = 0,
            Normal = 1,
            Close = 2,
        }
        public enum Name
        {
            待审核 = 0,
            正常 = 1,
            关闭 = 2,
        }
    }
}
